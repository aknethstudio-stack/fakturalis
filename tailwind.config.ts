/**
 * Tailwind CSS v4 configuration (ESM)
 *
 * In Tailwind v4 the config file is optional. Most customization (colors, spacings,
 * breakpoints, etc.) should be done in CSS using:
 *
 *   @import "tailwindcss";
 *   @theme { ... }
 *
 * This file is only necessary if you want to register plugins or use advanced options.
 * Deprecated fields like `purge`, `content`, `darkMode`, and `variants` have been removed in v4.
 *
 * Docs: https://tailwindcss.com/docs/configuration
 */

/** @type {import('tailwindcss').Config} */
const config = {
  // Add Tailwind plugins here if needed, e.g.:
  // import forms from '@tailwindcss/forms'
  // plugins: [forms],
  plugins: [],
};

export default config;
